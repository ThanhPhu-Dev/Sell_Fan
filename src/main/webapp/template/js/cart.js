document.addEventListener('DOMContentLoaded', () => {
    console.log('loaded')
})

const addQuantityClicked = (e) => {
    console.log('add');
}

const reduceQuantityClicked = (e) => {
    console.log('reduce');
}